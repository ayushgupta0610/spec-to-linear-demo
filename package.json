{
  "name": "spec-to-linear",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "preview": "node --experimental-strip-types src/cli.ts preview examples/analytics-inspector.json",
    "test": "node --experimental-strip-types --test tests/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "engines": { "node": ">=22.18" },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  }
}
